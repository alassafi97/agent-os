---
name: metis
description: Run Metis — research & analysis engine. Any question or topic → structured report + SVG diagram as a clean HTML file
---

Read the full agent spec at `agents/utils/metis.md` and execute it completely.

Also read `config.md` for business context before writing any analysis.

User input: $ARGUMENTS
