---
name: list-agents
description: Display the full Agent OS catalog with descriptions and routing guidance
---

Read `agents/AGENTS.md` and present it to the user in a clean, scannable format.

After showing the catalog, add:

"Tell me what you need and I'll pick the right agent. Or say 'run [agent name]' to run one directly."

Also check `.env` to see which API keys are configured, and note which agents are ready to run vs. which need keys set up first.
